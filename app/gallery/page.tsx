export default function GalleryPage() {
  return (
    <main className="page">
      <h1>Gallery (migration target)</h1>
      <p>This route is now served by Next.js. Content migration is next.</p>
      <p><a href="/legacy/gallery.html">Open the legacy gallery meanwhile</a>.</p>
    </main>
  );
}
