export default function AdminPage() {
  return (
    <main className="page">
      <h1>Admin (migration target)</h1>
      <p>This route is now served by Next.js. Admin UI migration is next.</p>
      <p><a href="/legacy/admin.html">Open the legacy admin meanwhile</a>.</p>
    </main>
  );
}
